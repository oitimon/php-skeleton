language: generic

before_install:
  - docker-compose up --build -d php

script:
  - docker exec -it php-skeleton pecl install xdebug
  - docker exec -it php-skeleton composer test
  - docker exec -it php-skeleton php travis/coverage-checker.php build/logs/clover.xml 80
