language: php

services:
  - docker

before_install:
  docker build -t oitimon/php-skeleton .

script:
  - docker run --rm --interactive --tty --volume $PWD:/app composer update && composer test && php travis/coverage-checker.php build/logs/clover.xml 80
